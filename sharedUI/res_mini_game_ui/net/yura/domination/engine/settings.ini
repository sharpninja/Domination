# core Game settings! for iOS and Android

name=Domination

# app version comes from the manifest on iOS/Android
# this version is displayed on the phone as the "Game Engine version"
version=1.2.7

defaultmap=luca.map
defaultcards=risk.cards

# not used
VERSION_URL=http://yura.net/AndroidDominationVersion

